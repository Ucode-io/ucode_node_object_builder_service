// const tabStore = require("../storage/mongo/tab");
// const catchWrapService = require("../helper/catchWrapService");

const tabService = {
    // Create: catchWrapService(`service.tab.create`, tabStore.create),
    // Update: catchWrapService(`service.tab.update`, tabStore.update),
    // GetSingle: catchWrapService(`service.tab.getByID`, tabStore.getByID),
    // GetList: catchWrapService(`service.tab.getAll`, tabStore.getAll),
    // Delete: catchWrapService(`service.tab.delete`, tabStore.delete),
};

module.exports = tabService;